#include <iostream>

// use const on the parameters not to be changed, and pass the others by
// reference so they CAN be changed
void minmax(const int a, const int b, int &smallest, int &largest);

int main()
{
  return 0;
}
